# V3 — Accounts tracker (traders portfolio). COMPRESSED Aug 12.
# Read V1.md + V2.md first. Live at /accounts_tracker; backend/traders/.

GOAL: simple TABLE dashboard for ~3 wallets (his own, brother, friend) because
Polymarket's portfolio analytics are poor. Wallets shown separately, no charts.
Billed as its own piece; ITEMS 4 (entry/exit alerts) AND 6 (custom tags) are
billable extras the client asked to be priced — the USER sets prices.

## ARCHITECTURE (backend/traders/, one job per file)

  fees.py     flat schedule — FALLBACK ONLY (see FEES)
  dataapi.py  shared-httpx data-api client; fetch_fills = takerOnly double-
              fetch, maker/taker classified AT INGEST (only works inside the
              10k window); fetch_fill_fees = exact cash fees from activity
              usdcSize; fetch_resolution = CLOB /markets/{cid} winner flags;
              fetch_value 3s timeout (flaky + cosmetic)
  store.py    trader_accounts / trader_fills (role+fee stored FOREVER — the
              10k offset cap makes our store the only depth) / trader_tags
              (tag on the ROUND TRIP = (account, asset); all_tags = vocab) /
              trader_resolutions (persisted — closed markets are final;
              without it every restart re-fired a ~200-lookup CLOB storm that
              Cloudflare 403'd and losses silently vanished)
  engine.py   weighted-average matching per (account, asset) — matches the
              avgPrice Polymarket shows; partial exits; EPS 0.01 shares
  service.py  sync throttle 120s + _match_cache 10s WITH in-flight lock
              (summary/open/closed share one pass; sync invalidates);
              _positions 20s, _act_cache 30s, _res_cache + semaphore(8);
              _redeems_as_sells; ghost resolution two-pass; _alive() filter;
              peak_after (our ticks first, CLOB fallback, _sustained_max)
  api.py      /api/traders: add by URL/0x, summary, open, closed, activity,
              tags vocab, tag toggle, sync(force), peak, delete

## MATCHING / P&L RULES (all settled with the user — do not relitigate)

- WIN = round trip netted > $0 AFTER fees. Win rate = wins / closed trips.
- Fees included in net; DISPLAY headline P/L is BEFORE fees (reconciles with
  the profile page, which ignores fees) with "(X after fees)" beneath.
- REDEEM = exit priced by CASH RECEIVED: price = usdcSize/size clamped 0..1.
  NEVER assume $1 — Polymarket sweeps LOSING positions as full-size REDEEMs
  with usdcSize=0 (was size=0 before Aug 11); the $1 assumption made phantom
  100c wins. REDEEM rows carry EMPTY asset — resolve via (conditionId,
  outcome) map from stored BUY fills; size<=0 redeems skipped.
- /positions DROPS resolved-lost holdings ("ride-to-zero losses vanish") —
  residual net-long trips absent from /positions resolve via CLOB winner
  flags: winner -> $1, loser -> $0; unknown stays open (conservative),
  retries 600s, persisted once closed. curPrice==0 rows are dead regardless
  of redeemable, excluded from open side (else double-counted).
- FEES: exact from activity usdcSize (usdc − p*s buys / p*s − usdc sells;
  makers ≈ 0). /activity AGGREGATES partial fills that /trades splits — a
  transaction's leftover cash fee is apportioned across unmatched fills
  weighted by size*p*(1−p) (_assign_fees). update_fees re-stamps stored
  fills every sync (self-healing). Old flat schedule (takers only; sports
  .05, crypto .07, politics .04, geo 0; fee = sh*rate*p*(1−p)) = fallback for
  fills beyond the activity window; rates changed mid-2026 (3%->5% sports) so
  the schedule can NEVER be trusted across time. CLOB maker_base_fee fields
  are junk. Profile P/L (user-pnl-api) is PRE-FEE; reconcile gross vs it
  (~$15 drift = mark timing).
- Peak-after-exit: _sustained_max (pairwise min of consecutive ticks) — a
  dying market's midpoint snaps to ~50c for one final poll (empty ask side);
  lone spikes are artifacts. Phrasing branches on close_reason.

## FRONTEND (AccountsTracker.jsx + traderAlerts/traderHidden/matchNotes-style
   localStorage modules — ALL follow read-fresh + storage-listener rule)

- Closed-trades CATEGORY SUMMARY sits at the TOP of the section, above the
  first row (Aug 13: it shipped under the table and on a 495-trade category
  he had to scroll past everything to reach the one number he opened the page
  for). Net is the headline in its P/L colour, then wins made / losses cost /
  WIN RATE / staked / ROI / fees — all following the active filters. Wins and
  losses are reported APART, not netted: a category that churns hard both ways
  and a quiet one net the same, and the split is what he asked for. Bucketed
  by each row's own WIN/LOSS pill so the money always agrees with the header
  counts. Win rate is per-filter (the stat card up top is account-wide and
  cannot answer "am I good at tennis"). The column-aligned totals row stays in
  the tfoot where it lines up with its columns.
- Stat cards (pre-fee headline + after-fees subtitle; fees card explains
  "1.3% of $135k traded — charged per trade, not on profit"); period chips
  Today/Yesterday/7d/All (client-side windows, ET days, rolling 168h;
  portfolio/unrealized/open always "right now"); item 5 (cards react to
  FILTER selections) quoted, awaiting go-ahead.
- Open table: Potential profit column (shares−cost), tfoot totals following
  filters + collect-if-all-win glance line. Closed: WIN/LOSS outline pills,
  tinted rows, resolved badges, peak block, per-row fees.
- Tags: fixed list (Bounce, Average Down, One/Two/Three Runs Behind, First/
  2nd Innings Bet, Bottom Innings, Favorite, Underdog, AFG) + custom vocab
  (create by applying; vocab = distinct trader_tags; removing last use drops
  it). Averaged-down auto-detected.
- Alerts: per-position price alerts + ACCOUNT-WIDE rule ({above,below} per
  account — "exit at 70c" workflow; per-position overrides account for that
  position, fired keys namespaced own|acct) + entry/exit toasts (watermark
  per account so first load never floods). Watcher: 30s tick (matches
  activity cache TTL), tab-visible only, ~50s worst latency; per-account
  MUTE bell silences everything; alerts only fire while a tab is open —
  server-side push = separate billable work. Toasts live 40s (test timing!).
- Hide/unhide rows (cosmetic: stat cards unaffected), blue ↗ chips to
  Polymarket profile/event pages, themed ConfirmDialog on account removal,
  session cache (module memo) = instant revisit paint.
- Open-orders watcher (client idea): open orders are PRIVATE — needs his CLOB
  API creds (which can also place orders) and only works for his own
  accounts; fills already alert today. Bigger quotable feature, unbuilt.

## "COMEBACK SETUP" TRIGGER — BUILT + LIVE Aug 13 (backend/comeback/)

Tag + alert when the away side, protecting a 1-run lead OR TIED (client
added ties on build day) at inning >= 8 (7/9 selectable) with home batting
or about to bat, brings in a NEW pitcher who fails a fatigue check: pitched
yesterday / recent WHIP > 1.35 over last 10 apps / >= 25 pitches previous
outing (>= quality_min of those, default 1; consecutive-days upgradeable to
mandatory). BILLABLE — bill like items 4/6; USER sets the price.

WHY IT EXISTS: latency arbitrage, not analytics. SPEED IS THE METRIC — a
correct alert 90s late is worthless. Every design choice below serves that.

HOW (one module per file): detector.py runs every 10s over mlb_live
.live_states() — the EXISTING 3s in-process cache, so watching costs zero
upstream. Tracks defense.pitcher.id per (game, side) between ticks (ids now
ride the light linescore state; names collide, ids don't). First sighting =
baseline, so a restart can never false-fire. NOT GUMBO — the spec named
/feed/live but that is the one endpoint we never call (V2 house rule); the
linescore names the incoming reliever DURING the Middle break, ~2min before
the boxscore counter, and firing during the break is deliberate (that is
when changes are announced; waiting for the half to flip wastes the window).
pitchers.py: gameLog cached 3h, fetched ONLY when a change is being
evaluated; unreachable log = NOT fired (never unverified); today's own
outing excluded from "previous work". store.py: comeback_triggers logged
FOREVER (payload = score/inning/reasons/config-at-trigger/prices AT trigger,
live CLOB when available, oriented to MLB home/away BY NAME — screener rows
are away-first); UNIQUE(game_pk, pitcher_id) = fire once, a second change in
the same game is its own trigger. Config = one JSON row, every threshold
editable in the UI (⚡ Comeback dialog on baseball screener), unknown keys
refused. api.py: /api/comeback active|ack|config|log — /log is the
review/backtest export.

UI: row flashes red via tr.comeback-flash CSS animation (outranks inline
highlight backgrounds; tds transparent). ⚡ COMEBACK SETUP tag shows the
reliever + HOME TEAM PRICE CAPTURED AT TRIGGER (what he could act on, not
what it drifted to). Toast + triple situation tone, including on page load
if unacked. ✓ Check acks SERVER-SIDE (survives reload, all windows); tag
stays quietly for the day. PLAYWRIGHT TRAP: the infinite animation makes
actionability checks hang forever — use force:true / DOM click on flashing
rows.

29-check suite in scratchpad (quality maths, every criteria branch, price
orientation flip, fire-once, ack, config validation).

PRICE TEXT (the cut-off sentence, resolved Aug 13): value bands on the HOME
side's LIVE price while home bats at inning >= 8 (comebackValue.js, pure fn,
rendered in the home priceCell under CLEAR FAVORITE). Down-1: <=20 Strong
value / <=25 Decent / <=30 Marginal-fair / else No edge or negative. Tied:
<=50 / <=55 / <=60 / else. <= on each edge so 20.5c lands in "21-25".
"Bottom 8th" read as inning >= 8 (9th/extras keep the label — assumption,
flagged to the user). Tiers colour-coded: solid green / outline green / grey
/ red. Follows the LIVE price by design ("when the price hits") — unlike the
frozen trigger tag next to it.
STILL OPEN: alerts only fire while a tab is open — server-side push =
separate billable work, ALREADY FLAGGED.
OUTCOMES (built Aug 13, outcomes.py, 60s job): each trigger row grows
`outcome` = {samples: {5,15,30 min -> home CLOB mid, fetched DIRECT from
/midpoints so it lands with no viewer; a missed moment = honest null, never
a wrong-time price}, final: {score, home_won} once the schedule says Final}.
Pending ages out after 8h (postponed games must not pin the job). /log
answers both "was there edge" and "did the comeback land" on its own.
Possible v2s: pre-warm likely-reliever gameLogs pre-game, score range as a
config (down-2 arguably fits his thesis).

## LIMITATIONS (measured — cite when scoping)

- /trades 10k offset cap (offset=20000 -> 400). Store from day one.
- prices-history: 1-min bars last ~24h, 10-min beyond — spikes invisible;
  OUR ticks (1s/5s) beat it for tracked markets, source labelled in UI.
- Name lookup unreliable (leaderboard only) — parse 0x from pasted URL.
- Deposits/withdrawals/cash = on-chain only (not in any Polymarket API; the
  404 list: /balance /deposits /withdrawals /networth /portfolio /pnl
  /stats). Cash IS public on Polygon if ever wanted.
- polymarketanalytics.com: their API is one authed endpoint with 8 fields,
  no positions/trades/win-rate — paying removes zero work. Their site runs
  its own indexed store (429k trades > 10k cap). Don't reconcile against
  them; the client's profile is ground truth (minus fees).
- MERGE/SPLIT netting, lowest-after-entry, CSV export: deliberately unbuilt.
- Client still owes the three real profile URLs.

## GOTCHAS

- Restart used to dip closed-count/realized while resolution cache re-warmed
  — fixed by trader_resolutions persistence; failures log
  "trader resolutions: N of M failed".
- Cloudflare 403s burst CLOB lookups (urllib always 403s — UA); ratchet
  retries + persistence handle it.
- Testing wallet 0x87f8…de3 = "Testing"; accounts 2/3/4 on prod.

## SOCCER: BIG-5 LIVE DATA + THE 0-0 FAVORITE ALERT (Aug 16, deployed)

Client spec: EPL/Bundesliga/La Liga/Serie A/Ligue 1 + clear pre-match
favorite (>=60c one side) + still 0-0 at 60' -> alert "just like baseball".
backend/football/: client (API-FOOTBALL v3, x-apisports-key, league ids
39/78/140/135/61), matcher (EXACT normalized league labels — substring
matching leaked "Brazil Serie A" / "Premier League (Kazakhstan)" / "Russian
Premier League"; team aliases psg/bayern munich/spurs/...), prematch (CLOB
1-min bar at kickoff — the screener cache would give LIVE prices by 60'),
detector (PURE check(), testable keyless), live (cache job: gates on our
own screener kickoffs -> ZERO requests while no big-5 match in its window;
1 live-fixtures call/min + 1 statistics/fixture/2min + 1 final/trigger),
store (football_triggers UNIQUE(fixture_id), config JSON: enabled,
min_favorite_cents 60, min_minute 60, skip_if_favorite_red_card false).
Row shows live strip (minute/score/possession/shots(SoT)/reds) + red flash
until CHECK (reuses comeback-flash CSS), toast+sound, FootballDialog config,
outcomes recorded (final + favorite_scored). 43/43 tests (scratchpad
test_football.py), fake-API end-to-end included.
KEY CONFIGURED (Aug 16): user's key in local + prod .env (NEVER in git —
it briefly landed in .env.example, scrubbed before any commit). Verified
END-TO-END on prod with a real live match: Racing Santander 2-2 Villarreal
86' (La Liga) — fixture polled, stats flowing (poss 43-57, shots 17(4)-
12(7)), matcher linked Polymarket row 'Real Racing Club vs. Villarreal CF'
-> slug lal-rrc-vil-2026-08-16, no trigger (2-2, correctly). Quota math:
0 req on quiet days; big Saturday ~1.5-2.2k req at defaults -> free tier
(100/day) insufficient, Pro (7.5k/day) comfortable; intervals are .env
settings (football_poll_seconds / football_stats_seconds). Aug 16 sweep: Polymarket lists La Liga (18
events) only so far; EPL/others map when their events appear — if a label
variant is unexpected, add it to matcher._BIG5_LABELS (exact match only).

## OUTAGE (Aug 19 2026): "baseball screener shows nothing / + does nothing"

SYMPTOM (client-reported): empty baseball table, + button dead, no live
scores. NOT a server or network problem — curl to statsapi from the same
VM worked fine throughout, and Gamma/CLOB kept running (screener rebuilt
on schedule).
ROOT CAUSE: the shared httpx client in backend/mlb/client.py kept
keep-alive sockets to statsapi that went HALF-OPEN (request written, no
reply ever comes — httpx cannot detect this). Every MLB call then burned
the full 8s timeout. The 3s mlb-live poll could never finish: 3,334
"maximum number of running instances reached" skips in 2h, 4 sockets in
CLOSE-WAIT. Downstream: live states froze AND screener_cache rebuilt with
game_pk NULL on all 97 rows (_attach_game_pks needs mlb.schedule) — and
EVERYTHING in the baseball UI keys off game_pk, hence the empty table and
the dead + button. Broke 00:28 UTC, ran ~1h until restart.
WHY IT NEVER SELF-HEALED: _http() only rebuilt the client when
`is_closed` — a wedged-but-OPEN client lived forever.
FIX (commit "MLB: rebuild the connection pool when it wedges"):
_WatchedTransport subclass counts consecutive httpx Timeout/NetworkError
(all ~20 call sites share the client, so one place covers them all);
_http() drops and rebuilds the whole pool after 3 in a row, closing the
stale one in the background. Timeout split (connect 3s / read 8s),
keepalive_expiry 30s. 12/12 tests in scratchpad
test_mlb_client_healing.py (wedge -> recycle, lone blip -> no recycle,
auto-recovery without restart).
DIAGNOSTIC RECIPE for "feature X shows nothing": check
`journalctl | grep -c 'maximum number of running instances'` (job pileup)
and `ss -tnp | grep pid=<uvicorn>` for CLOSE-WAIT — a stuck poll job with
an empty exception message ("fetch failed:" with nothing after it) means
a timeout, i.e. a wedged pool, not an API rejection (which would carry a
status code).
SAME RISK ELSEWHERE: gamma/clob/football all use long-lived shared
clients with the same pattern — none has wedged yet; port this recycle
logic if one does.

## ACCOUNTS: MLB POSITIONS DEEP-LINK TO MLB.COM (Aug 19)

Client: "add a link that takes the user to the MLB.com link of that game,
MLB positions only". A "Game" column on BOTH the open and closed tables
holds an MLB-navy button (mlbLink() in AccountsTracker.jsx); non-MLB rows
render an empty cell. URL form: https://www.mlb.com/gameday/{gamePk} —
redirects to the pretty /gameday/white-sox-vs-cubs/2026/08/18/{pk}.
BACKEND: GET /api/mlb/game-links?slugs=a,b,c -> {links:{slug:{game_pk,url}}}.
Resolution REUSES backend/mlb/timeline.resolve_game_pk (already existed:
screener_cache match first, then slug abbrs + date with _ABBR_ALIASES so
oak->ATH, ari->AZ). Added an optional sched_memo arg so a batch costs ONE
schedule call per DATE, not per slug — never cache client.schedule
globally, mlb/live.py reads live status from it.
CACHING: new table mlb_game_pks(event_slug PK, game_pk, resolved_at) —
permanent, because a finished game's pk never changes. ONLY successful
matches are stored: caching a miss during an MLB outage would make the
gap permanent. Per-request ceiling MAX_NEW_GAME_LINKS=12 so a page load
cannot fan out across the client's 1,141 distinct MLB slugs; the rest
fill in on the next poll as the frontend re-asks for what is still
missing. Frontend asks only for the slugs ON SCREEN.
GOTCHA (verified, not a bug): a slug dated 2026-05-24 resolved to a game
MLB.com dates Aug 17 — that game was POSTPONED and made up, and MLB keeps
the SAME gamePk while the schedule for the original date lists it with
status Postponed + rescheduleDate. So the link is right; don't "fix" it.
Tests: scratchpad test_mlb_game_links.py, 12/12 (real slugs incl. a May
one, cache hit costs 0 requests, 1 fetch per date, MLB-only, misses
uncached).
TESTING NOTE: the MLB client is module-level and long-lived, so a test
must run all phases in ONE asyncio.run — a second run() hands it
connections bound to a closed loop ("Event loop is closed").

## ONE-CLICK "DASHBOARD" BUTTON (Aug 19)

Client: a button on the screener AND the accounts tracker that adds that
prop/position/game to the main tracker, backfills history immediately,
starts tracking, and takes him to the chart page.
POST /api/track-and-chart {slug, condition_id?} -> {market_id, question,
event_title, closed}. Does upsert_event + add_market + scheduler.sync_jobs
+ create_task(backfill_market) — i.e. the same work /events/track does,
for ONE market, with no picker.
WHICH MARKET: screener row = the match's winner market via _winner_market()
— GOTCHA: spreads carry kind 'team' with team-name outcomes exactly like
the moneyline, so they must be excluded by question text (reuses
screener_cache._NON_MONEYLINE). Three-way soccer has NO moneyline market
at all (three yes/no "Will X win" + draw), so it falls back to a win
market with 'draw' excluded. Accounts row = its own condition_id, so a
position charts ITS prop; all 15 of the client's NFL-champion positions
share ONE event slug, which is why condition_id is essential. Lookup also
tries the "-more-markets" twin where extra props live.
SURFACED condition_id on trader rows: engine._close() (the trip already
carried it) and service.open_rows() (p["conditionId"] from the positions
payload).
IDEMPOTENT: db.add_market returns the existing id and resumes tracking;
backfill_market skips a market that already has ticks. Pressing twice is
free.
FRONTEND GOTCHA: App renders the chart from markets.find(id === route.id),
so onOpenHistory must AWAIT refresh() before navigate() — otherwise
/market/<new id> falls through and renders the dashboard.
Verified on prod: MLB game -> 42,562 ticks backfilled (Aug 13 ->), each
NFL position -> ~8,850 ticks (a month), all tracking=1.
Tests: scratchpad test_track_and_chart.py 12/12 (moneyline not spread,
soccer win market, prop-specific, idempotent, 404s).

## MLB BOTTOM 8TH INNINGS PAGE (Aug 20, deployed)

Client spec: record every MLB game TIED at the middle of the 8th (top 8
done, bottom 8 about to start), show a table + accumulated stats, with a
Dashboard link and an MLB.com link under each game.
backend/bottom8/: store (bottom8_games, PK game_pk so re-opening is a
no-op), tracker (detect + follow), api (GET /api/bottom8 -> {rows, stats}).
Job "bottom8-tracker" every 10s next to the comeback detector.
TRIGGER: inningState == "Middle" and inning == 8 and runs equal — the SAME
signal the comeback detector uses (the break lasts minutes, 10s polling
cannot miss it). Fallback: bottom 8 with outs == 0 and still tied, so a
restart during the break does not lose the game.
FOLLOW until Final: home_high/away_high (max of sampled prices), B9 start
score + b9_home_price recorded ONCE at the middle of the 9th, extras_inning
(last inning when > 9), final score + winner.
KEY DOMAIN POINT: there is NOT always a bottom 9th — if the home team
leads after the top of the 9th the game just ends, so B9 columns stay "—".
That is why the client's own sheet shows "—" on a home win with no B9.
Scores are stored/displayed AWAY-HOME ("3–4" = away 3, home 4), matching
his sheet and the "AWAY @ HOME" game column.
PRICES: tracker calls live_prices.request(slug) so the game stays priced
with NO browser open (the poller only prices "wanted" slugs, WANT_TTL 90s).
REFACTOR (side effect, safe): the away-first side-alignment rule now lives
once in live_prices.game_prices(game_pk, mlb_home_name); comeback/detector
lost its private _prices_for copy. Screener MLB rows are away-first, so
sides are matched BY NAME — never duplicate that rule again.
GUARD: a suspended/postponed game stuck in 'tracking' for 12h is marked
'stale' and excluded from stats instead of being chased forever.
FRONTEND: chip "MLB Bottom 8th Innings" next to OVER (baseball only, its
own view, not a status filter) -> components/Bottom8Table.jsx. Stats count
FINISHED games only. Tests: scratchpad test_bottom8.py 36/36 (whole
lifecycle driven through fake caches: no live game or API key needed).

## BOTTOM-8 ANALYSIS TABLES (Aug 20, same day as the page)

Three blocks under the game list, all counting status='final' only so the
denominators match the headline stats:
1. "Was the price right?" — games bucketed by b8_home_price vs the actual
   home win rate. Client's bands 50-54/55-59/60-64/65-67 ALWAYS render
   (even empty); "Under 50¢" and "68¢+" appear only when non-empty, so an
   out-of-range price is never silently dropped.
2. "Price Movement After Bottom 8 Entry" — home_high >= 80/85/90/95/100,
   plus home_low <= entry*(1-0.25|0.50). NOTE: the client's mock numbers
   were internally inconsistent (85¢ listed MORE games than 80¢, which is
   impossible); implemented the correct monotonic logic and the test
   asserts the ladder never inverts.
3. "Team Performance — Tied Entering Bottom 8" — per HOME team: record,
   win %, avg entry price, reached-80¢/95¢ rates, extras count + extras
   win %. Sorted by games played.
SCHEMA: the drawdown rows needed data we were not keeping -> added
home_low/away_low, with an ALTER TABLE migration in store.init() because
the table already existed on prod (0 rows at the time, so no gap).
Tests: scratchpad test_bottom8_tables.py 27/27 — rows seeded directly with
known numbers so every percentage is checkable by hand.


## ACCOUNTS TRACKER IS PER-USER NOW (Aug 21, client request)

Client: wallets he adds must only show/alert on HIS login; his brother's
on his. Was one global trader_accounts list; the frontend polls/toasts
over GET /api/traders, so scoping THAT list per signed-in user fixed
visibility and alerts in one move.
DESIGN: trader_accounts.owner_id (users.id). NULL = legacy/unclaimed ->
visible to EVERYONE with a SHARED badge; re-adding the same wallet CLAIMS
the row (UPDATE owner, same id) so the fill history — which reaches beyond
Polymarket's 10k-row API window — follows the account. wallet UNIQUE
relaxed to UNIQUE(wallet, owner_id) so two users can track the same trader
independently (separate rows, duplicate fills accepted). Route guard in
_account_or_404: another user's account answers 404 (no existence leak);
current_user==None (health token, background jobs, AUTH_ENABLED=false dev
returns id 0 dev user — NOT None) => unrestricted.
DEPLOY INCIDENT — TWO lessons, both mine:
1. DROP TABLE parent under PRAGMA foreign_keys=ON => IntegrityError:
   trader_fills/tags REFERENCE trader_accounts(id). get_db() enforces FKs,
   so a rebuild migration MUST run on its own raw sqlite3 connection (FKs
   default OFF per connection). The crash LOOPED the service (init runs at
   startup) — prod was down ~6 min.
2. executescript COMMITS statement by statement: the failed first run left
   trader_accounts_v2 behind, and the retry died on "already exists" —
   a rebuild migration must start with DROP TABLE IF EXISTS <staging>.
Verified after: 8 accounts ids 2-9 owner NULL, 45,128 fills, 0 orphans.
Tests: scratchpad test_trader_owners.py 15/15 (migration path, isolation,
same-wallet-two-users, claim-on-add, 404 guard, healthcheck bypass).

## THE DELETE INCIDENT (Aug 24) — tracked accounts
Any signed-in user could DELETE an unclaimed shared account (owner NULL rows
pass _account_or_404 by design, and DELETE wasn't excluded); delete_account
hard-wiped fills+tags for everyone. Client deleted a test account and lost
weeks of history. 6 accounts (ids 1,4-8) were already gone before journald's
horizon (Aug 22); id 3 deleted 18:03 Aug 24, re-added 4 min later (~11.3k of
its fills came back via the API's 10k window; the rest is unrecoverable — no
backups existed, dead DB pages already reused, VM service account can't read
GCP snapshots). Deleted wallets are NOT recoverable server-side — client
re-adds from his own records; claim-on-add + resurrection restore what the
API still serves.
FIXES (deployed Aug 24): deletes are SOFT (deleted=1; fills/tags kept;
re-adding the wallet resurrects the row, richest-history first), owner-only
(shared rows → 409 "claim first"), every delete logged with wallet+user.
NEW: nightly db-backup job (backend/database/backup.py, scheduler id
db-backup, +900s after boot) dumps the un-recoverable tables to
/opt/polymarket-tracker/backups/precious-YYYYMMDD.sql.gz, 14 kept. Restore:
executescript into fresh db. Tests: scratchpad test_soft_delete.py 21/21.
LESSON: any route that destroys data must check ownership EXPLICITLY even
when a shared-visibility guard exists — visibility and destruction are
different permissions.
